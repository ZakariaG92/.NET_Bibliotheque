<Window x:Class="ClientApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientApp"
        xmlns:ViewModel="clr-namespace:ClientApp.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
           <Window.Resources>
        <ViewModel:Book x:Key="viewModelBook"></ViewModel:Book>
    </Window.Resources>
            <StackPanel DataContext="{StaticResource viewModelBook}">
        <Label Content="Client lourd"></Label>
        <TextBox x:Name="txtBox" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button x:Name="btn" Content="Click here" Click="btn_Click" ></Button>
        <ListBox x:Name="lb" ></ListBox>
        

    </StackPanel>
</Window>
